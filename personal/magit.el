;; load magit-forge

;;(unless (package-installed-p 'forge)
;;  (package-install 'forge))

;;(with-eval-after-load 'magit
;;  (require 'forge))

;; only use the one file for authentication
;;(setq auth-sources '("~/.authinfo"))
